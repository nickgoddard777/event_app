<h1><%= @category.name %></h1>
<p><%= @category.description %></p>

<% unless @category.events.empty? %>
	<ul class="events">
		<%= render @events %>
	</ul>
	<%= will_paginate @events %>
	<% if admin_signed_in? %>
		<%= link_to "Edit", edit_category_path(category.id) %>
	<% end %>
<% end %>
